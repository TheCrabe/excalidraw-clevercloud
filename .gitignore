# Per-app working dirs — each lives as its own deploy repo, follow the tutorial to (re)create
# (frontend/room/mcp are symlinks to forks; storage is bootstrapped from tutorial 02)
frontend
room
mcp
storage

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/.terraform.lock.hcl
terraform/terraform.tfvars

# Clever Cloud per-app link file (auto-generated by `clever link`)
**/.clever.json

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
