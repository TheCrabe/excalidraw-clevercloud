# Nested clones — each has its own remote on TheCrabe/excalidraw[-room|-mcp]
frontend/
room/
mcp/

# Storage backend deps (tracked: source + package.json, not built artifacts)
storage/node_modules/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/.terraform.lock.hcl
terraform/terraform.tfvars

# Clever Cloud per-app link file (auto-generated by `clever link`)
**/.clever.json

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
